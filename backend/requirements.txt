# AI Cloud Security Guardian — Python Dependencies
# Pinned for Render compatibility

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# Database — use psycopg2-binary (pre-compiled, no build tools needed)
sqlalchemy==2.0.35
psycopg2-binary==2.9.9

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Settings
pydantic-settings==2.5.2
pydantic==2.9.2
python-dotenv==1.0.1

# AWS SDK
boto3==1.35.30
botocore==1.35.30

# Machine learning — pin numpy first to avoid conflicts
numpy==1.26.4
scikit-learn==1.5.2
joblib==1.4.2

# HTTP client
httpx==0.27.2

# Logging
python-json-logger==2.0.7